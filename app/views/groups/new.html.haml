.chat-group-form
  %h1 新規チャットグループ
  = form_for @group do |f|
    - if @group.errors.any?
      .chat-group-form__errors
        %h2
          = @group.errors.count
          件のエラーが発生しました
        %ul
        - @group.errors.full_messages.each do |msg|
          %li
            = msg
    .chat-group-form__field.clearfix
      .chat-group-form__field--left
        = f.label :group_id, class: 'chat-group-form__label'
      .chat-group-form__field--right
        = f.text_field :name, class: 'chat-group-form__input', placeholder: 'グループ名を入力してください'
    .chat-group-form__field.clearfix
      .chat-group-form__field--left
        = f.label :add_chat_group, class: 'chat-group-form__label'
      .chat-group-form__field--right
        = collection_check_boxes(:group, :user_ids, User.all, :id, :Name) do |b|
          = b.label { b.check_box + b.text }
    .chat-group-form__field.clearfix
      .chat-group-form__field--right
        = f.submit 'Save', class: 'chat-group-form__action-btn'
