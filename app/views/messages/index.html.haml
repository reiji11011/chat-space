= render 'shared/side-bar'

.app-main
  .app-main__header
    .app-main__header-content
      .app-main__header-content-group-name
        = @group.name
      .app-main__header-content-edit-btn
        Edit
    .app-main__header-member
      MEMBER:
      - @group.users.each do |u|
        = u.Name

  .app-main__body
    .app-main__body-ls
      .app-main__body-ls-msg
        - @group.messages.each do |msg|
          .app-main__body-ls-msg-name
            = msg.user.Name
          .app-main__body-ls-msg-time
            = msg.created_at
          .app-main__body-ls-msg-detail
            = msg.text

  .app-main-footer
    .app-main-footer__content
      = form_for [@groups, @message], url: { action: :create } do |f|
        .app-main-footer__input
          = f.text_field :text, placeholder: 'type a message', class: 'app-main-footer__input--type-msg'
          %label.app-main-footer__input--icon
            = f.file_field :image, style: 'display:none'
            %i.fa.fa-file-image-o{ aria: { hidden: 'true' } }
        = f.submit 'Send', class: 'app-main-footer__content__send-btn'
